/**
 * Solved on Feb 10, 2026, 9:40 AM
 */

            res.add(s);
            return;
        }

        if (openP < n) {
            dfs(openP + 1, closeP, s + "(", n, res);
        if (openP == closeP && openP + closeP == n * 2) {
    }

    private void dfs(int openP, int closeP, String s, int n, List<String> res) {
        }

        if (closeP < openP) {
            dfs(openP, closeP + 1, s + ")", n, res);
        }
    }    
}
